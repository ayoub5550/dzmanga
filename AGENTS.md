# ملاحظات لأي AI agent يكمّل تطوير هذا المشروع

اقرأ `README.md` أولاً (البنية، الـAPI، القرارات المتعمدة). هذا الملف إضافي،
عملي أكثر، لتفادي الأخطاء الشائعة عند المتابعة.

## المصادر (مهم)

التطبيق الآن ثنائي المصدر: **مانجا العاشق (3asq)** افتراضياً + **MangaDex**.
كود العاشق كله في `src/sources/asq.js` ويعتمد على تحليل HTML بـregex (قالب
Madara). إذا غيّر الموقع قالبه ستفشل الدوال بصمت وتُرجع مصفوفات فارغة — ابدأ
التشخيص من `parseCards` / `parseChapters` / `parsePages` هناك. نقاط مفيدة:
`/manga/?m_orderby=latest|views`، `/?s=..&post_type=wp-manga` (قالب نتائج
مختلف: `row c-tabs-item__content`)، وقائمة الفصول عبر
`POST /manga/<slug>/ajax/chapters/`. صور 3asq تمر عبر `/img` بـReferer الموقع
نفسه.

لا تُضف Comick (`api.comick.dev`) كمصدر قراءة: عناوينه العربية مرآة MangaDex
بلا صور مستضافة (تحققنا 2026-08-21).

## قواعد يجب اتّباعها

- **لا تغيّر `LANG` من `'ar'`** في `src/server.js` دون تأكيد صريح من صاحب
  المشروع. التطبيق موجّه للعربية عمداً، وليس نسخة عامة تدعم كل اللغات.
- **لا تُدخل حسابات مستخدمين (auth) كاملة (Google/Facebook OAuth، SMTP
  للتحقق بالبريد...) دون سؤال صاحب المشروع أولاً.** سبق أن طلب هذا لمشروع آخر
  (marketyDz) ثم تراجع؛ يفضّل النطاق البسيط. إذا احتجت "حفظ تقدم القراءة"
  فابدأ بمعرّف جهاز عبر كوكي بسيط، ليس نظام تسجيل دخول كامل.
- **حافظ على الخفة**: لا تُضف framework فرونت-إند (React/Vue) أو حزمة كبيرة
  دون داعٍ واضح. القرار الحالي: HTML/CSS/JS عادي عمداً ("سريع وخفيف كالعادة"
  هو طلب صريح من صاحب المشروع في كل مشاريعه).
- **لا قاعدة بيانات بعد** — إذا أضفتها، اتبع نفس قاعدة مشاريعه الأخرى على
  الـVPS: `better-sqlite3`، مسار البيانات عبر `process.env.DATA_DIR`، بدون
  ORM ثقيل.
- عند النشر على الـVPS اتبع نفس القالب المستخدم لبقية تطبيقاته:
  `/opt/dzmanga/` + `dzmanga.service` (systemd) + nginx site. **لا تلمس أو
  تعيد تشغيل تطبيقات أخرى على نفس الـVPS لم تُنشئها أنت.**
- تطبيقاته تعمل على `http://` عادي (بدون HTTPS/domain بعد) — أي ميزة تتطلب
  "secure context" (مثل `navigator.clipboard`) ستفشل بصمت؛ أضف بديلاً
  (`document.execCommand('copy')` أو مكافئه) إن احتجت ميزة كهذه.

## أخطاء شائعة عند اختبار محلياً

- عمليات Node الخلفية تموت بين استدعاءات bash منفصلة في بيئة Viktor —
  شغّل الخادم وأوامر `curl` للاختبار في **نفس الاستدعاء**.
- لا يوجد متصفح Chromium مثبّت محلياً في هذه البيئة الرملية (playwright غير
  مهيأ بمتصفح) — الاختبار الأسرع هو عبر `curl` على مسارات `/api/*` مباشرة،
  أو رفع التطبيق فعلياً على الـVPS واختباره بمتصفح فعلي على الرابط الحي.

## MangaDex قد تحظر IP الـVPS مؤقتاً

لاحظنا (2026-08-17) أن إعادة تشغيل `dzmanga.service` عدة مرات متتالية خلال
دقائق قليلة (كل مرة تشغّل بناء فهرس الكتالوج الكامل من جديد، مئات الطلبات
دفعة واحدة) يمكن أن يجعل MangaDex تحظر IP الخادم مؤقتاً على مستوى الشبكة
(الطلبات تفشل بـ`TypeError: fetch failed` / `SocketError: other side
closed`، بينما `/ping` يبقى يعمل — دليل أن الحظر انتقائي على مسارات الـAPI
الفعلية وليس عطلاً عاماً). هذا **حظر مؤقت من جهة MangaDex، وليس خللاً في
الكود** — يزول تلقائياً بعد فترة تهدئة (دقائق إلى ساعة تقريباً حسب ملاحظتنا).

الحماية الموجودة حالياً في `cachedFetchJson`:
- إعادة محاولة تلقائية بتأخير (backoff) عند 429 أو 5xx أو فشل الشبكة (حتى 4
  محاولات).
- تهدئة 60 ثانية (`BUILD_RETRY_COOLDOWN_MS`) قبل إعادة محاولة بناء الفهرس
  الكامل بعد فشل — لتفادي تكرار الضغط على MangaDex أثناء الحظر.

**نصيحة عملية:** تجنّب إعادة تشغيل الخدمة أكثر من مرة كل بضع دقائق عند
النشر المتتالي؛ اجمع عدة تعديلات في نشرة واحدة قدر الإمكان بدل إعادة التشغيل
بعد كل سطر كود.

**تحديث (2026-08-17):** أضفنا طبقة أخيرة في `cachedFetchJson`: إذا فشلت كل
محاولات الاتصال المباشر بـ`api.mangadex.org` (الحظر ما زال فعّالاً)، يعيد
الكود المحاولة عبر `https://r.jina.ai/<url>` (وسيط قراءة مجاني بدون تسجيل،
IP مختلف تماماً) مع `Accept: application/json`، ثم يفكّ التغليف
(`data.content`) ويحوّله لـJSON فعلي. هذا يعمل فقط لطلبات الـAPI (JSON) —
صور الأغلفة وصفحات الفصول تُحمَّل مباشرة من متصفح المستخدم (IP الخاص به هو،
وليس الـVPS) لذا لا تتأثر بحظر IP الخادم أصلاً. جرّبنا أيضاً corsproxy.io
(محظور 403) وIPv6 (غير متوفر على الخادم) ولم ينفعا. ملاحظة: `r.jina.ai`
مجاني بحد ~20 طلب/دقيقة بدون مفتاح — كافٍ كخط دفاع أخير، ليس كمسار أساسي؛
لا تحوّل كل الحركة إليه.

## أفكار جاهزة للتنفيذ التالي

راجع قسم "أفكار للتطوير القادم" في `README.md`.

## طبقة SEO (أُضيفت 2026-08-21)

- الـSPA بروابط hash → غير مرئية لمحركات البحث. الحل المطبق: مسارات
  `/manga/:id` تُرجع `index.html` بعد استبدال ما بين `<!--SEO:START-->` و
  `<!--SEO:END-->` بوسوم حقيقية للمانجا. **لا تحذف هاتين العلامتين من
  `index.html`** — الاستبدال regex عليهما في `server.js`.
- `sitemap.xml` يعتمد على `fullCatalog` (يُبنى بالخلفية بعد الإقلاع بدقيقة
  تقريباً) — بعد إعادة تشغيل مباشرة سيكون قصيراً ثم يكتمل، هذا طبيعي.
- الدومين مثبت في `PUBLIC_ORIGIN` (env أو الافتراضي dzmanga.dpdns.org).
- إعدادات nginx على السيرفر (gzip_proxied/http2/proxy_cache للصور) موثقة في
  README — إن غيّرت شكل روابط `/img` أفرغ كاش nginx: `rm -rf /var/cache/nginx/dzimg/*`.

## الإعلانات

- `src/public/ads.js` هو نقطة الدخول الوحيدة للإعلانات. لا تلصق سكربتات
  إعلانات مباشرة في `index.html` أو `app.js`.
- سياسة صاحب المشروع: تحقيق دخل مطلوب، لكن بانرات فقط — لا popunders،
  لا إشعارات push، لا redirects. لا تضع إعلاناً بين صفحات الفصل الواحد
  (فقط بين الفصول أو في الصفحات الثابتة).

## IndexNow
- ملف المفتاح `src/public/042d2601047241428a8c8adbbb758525.txt` يُستخدم لإخطار Bing/Yandex فوراً بالروابط
  الجديدة عبر `https://api.indexnow.org/indexnow`. عند إضافة مانجات جديدة
  كثيرة يمكن إعادة الإرسال بنفس المفتاح. لفهرسة جوجل: Google Search
  Console مفعّل (انظر القسم أدناه).

## Google Search Console (فُعّل 2026-08-21)

- الموقع `https://www.dzmanga.dpdns.org/` مضاف ومُتحقق منه في GSC بحساب
  صاحب المشروع (ayoubteke12@gmail.com)، والـsitemap (769 رابطاً) مُرسل
  وسحبه جوجل بدون أخطاء.
- ملف التحقق `src/public/google7c8012eedab88b29.html` — **لا تحذفه أبداً**
  وإلا يسقط التحقق من ملكية الموقع.
- عند إضافة صفحات كثيرة جديدة لا يلزم شيء — جوجل يعيد سحب الـsitemap دورياً.

## السمة الحالية: "لعبة بكسل" (2026-08-21 ليلاً)

- تاريخ السمات: ذهبي ← أزرق/أحمر (فارس) ← حبري ← بكسل (الحالية، من لقطة
  لعبة RPG أرسلها صاحب المشروع). **السمة تغيّرت 5 مرات — لا تعتبرها نهائية،
  وبعد أي تغيير بصري أرسل له لقطة شاشة دائماً.**
- الألوان في `:root` داخل `index.html`: ماء أزرق `#1386c9` (خلفية شبكة بكسل)،
  طوب رملي `#e8d29b` للبطاقات، نص بني داكن `#2b1e0e`، أخضر ورقي `#2f9e3f`،
  لمسة وردية `#f2b6cf`. حدود سميكة `#4a3617` وبلا استدارة إطلاقاً
  (`*{border-radius:0!important}`) وظلال صلبة بلا blur.
- بنية الـCSS: الأساس ثم كتلة `PIXEL GAME THEME` ثم كتلة `QA FIXES` في نهاية
  `<style>`. أي سمة جديدة: أضف كتلة override جديدة في النهاية بدل نثر
  التعديلات، **وراجع المكوّنات ذات الألوان الثابتة** (live-results،
  chips المصادر، `.legal`، `.skeleton`، `.dz-toast`، وواجهة القارئ عبر
  `body:has(.reader)`) لأنها لا تستخدم متغيرات CSS كلها.
- القارئ يبقى داكناً دائماً مهما كانت السمة (طلب صريح).
- طلب دائم من صاحب المشروع: **ظلال حبرية صلبة على كل الأيقونات**
  (`svg{filter:drop-shadow(...)}` في كتلة QA FIXES) — حافظ عليها في أي سمة.
- الشعار: `icon-192.png` مبكسل (مولّد بـPIL بترقية NEAREST) + wordmark بخط
  Press Start 2P. بانر المشاركة `src/public/og-banner.png` (1200×628) بنفس
  الستايل، مربوط بـog:image وtwitter:card في الرئيسية.
- **أي تغيير في ملفات الـshell (index.html/app.js/ads.js/أيقونات): ارفع رقم
  `SHELL` في `sw.js`** (حالياً `dz-shell-v6`) وإلا يبقى الزوار على النسخة
  القديمة من كاش الـService Worker.

## الصفحات القانونية

- مسارات SPA: `#/page/privacy|terms|dmca|contact` تُعرض عبر
  `renderStaticPage(slug)` والمحتوى في القاموس `LEGAL_PAGES` داخل `app.js`.
- فوتر `.site-footer` في `index.html` (يختفي داخل القارئ). DMCA: إزالة خلال
  72 ساعة، بريد التواصل ayoubteke12@gmail.com.

## حالة الإعلانات (2026-08-21)

- Adsterra، site id `5998901`، ثلاث وحدات: Native Banner `30856951`
  (الرئيسية)، Banner 300x250 `30857292` وBanner 320x50 `30857293` (صفحة
  المانجا؛ الاختيار حسب عرض الشاشة ≤520px). المنطق كله في
  `src/public/ads.js` (MutationObserver على `#app`).
- **مشكلة معلّقة:** سكربت الوحدة Native يرجع HTTP 403 — غالباً الوحدة ما
  زالت قيد التفعيل لدى Adsterra. تحقق أن الإعلانات تظهر فعلاً (ارتفاع
  الـslot > 0) قبل اعتبار الملف منتهياً؛ إن استمر 403 بعد 48 ساعة راجع
  لوحة Adsterra أو أنشئ وحدة بديلة.
- كلمات السر وبيانات الدفع ليست في هذا المستودع عمداً — عند صاحب المشروع.

## مهام معلّقة بانتظار صاحب المشروع / أفكار تالية

1. **بوت تيليجرام للنشر التلقائي** (أولوية الزوار الأولى): بانتظار أن ينشئ
   قناة + بوت من BotFather ويرسل التوكن. الفكرة: cron يقارن أحدث الفصول من
   `/api/latest` ضد آخر حالة محفوظة وينشر الجديد في القناة برابط القراءة.
2. حزمة منشورات فيسبوك جاهزة للنسخ (المجموعات تحظر البوتات — النشر يدوي).
3. على السيرفر: إضافة swap بحجم 2GB، تفعيل ufw، تعطيل دخول root بكلمة سر.
4. حفظ تقدم القراءة عبر SQLite + كوكي جهاز (اتبع قاعدة better-sqlite3 أعلاه).
5. تصفية "الشائع" من العناوين بلا فصول عربية مقروءة.
6. **لا تولّد زيارات مزيفة أبداً** مهما طُلب منك — تحظر حساب Adsterra
   وتصادر أرباحه. الترافيك الشرعي فقط (تيليجرام/فيسبوك/SEO).

## تحسين طريقة التصفّح (2026-08-22)

- صفحة `#/browse` لم تعد تستخدم الترقيم (pagination): البديل `mountFeed()` في
  `app.js` — تحميل تدريجي عبر `IntersectionObserver` (rootMargin 600px) مع زر
  «تحميل المزيد» كبديل، وحالة `{page, done, busy, count}`. الدوال القديمة
  `loadAsqPage/loadMdAllPage/paginationHtml/bindPageButtons` أُزيلت.
- `FEED_CACHE` (Map في الذاكرة، 8 مفاتيح) يحفظ HTML الخلاصة + موضع التمرير
  لكل مفتاح `source|view`؛ الحفظ يحدث عند أي نقر على رابط `#/`
  (`saveCurrentFeedScroll`) والاسترجاع في `renderBrowse`. استرجاع التمرير
  يعيد المحاولة حتى 25 مرة كل 100ms لأن الأغلفة تُحمَّل تدريجياً، و
  `history.scrollRestoration='manual'` مضبوط حتى لا يتعارض المتصفح معنا.
- شريط المصدر + التصنيفات صار لاصقاً (`.browse-bar`, `top:var(--hh)`) — قيمة
  `--hh` = ارتفاع الهيدر الفعلي تُحسب بجافاسكربت عند التحميل/تغيير المقاس.
  التصنيف النشط يُمرَّر تلقائياً داخل الشريط (`scrollIntoView`).
- صفحة المانجا: حقل «اذهب إلى رقم فصل» + زرّا «الفصل الأول»/«الأخير».
  **مهم:** التصفية تحفظ `idx` الأصلي — `groupChaptersByVolume` تحترم
  `c.idx ?? idx`؛ لا تُعِد ترقيم الفصول بعد أي تصفية وإلا فتح القارئ فصلاً خطأ.
- القارئ: `<select id="chSelect">` للانتقال بين الفصول من داخل القارئ.
  وعدّاد الصفحة يتجاهل الصور غير المحمَّلة (`offsetHeight > 0`) — قبل ذلك كان
  يظهر «15/15» عند فتح الفصل.
- زر «↑ العودة للأعلى» عام يظهر بعد 900px ويختفي داخل القارئ.

## المصدر الثالث: Team-X (مانهوا) — 2026-08-22

`src/sources/teamx.js` (بادئة المعرّف `tx:`) يقرأ olympustaff.com بـregex:
- `/series?page=N&genre=<اسم عربي>&type=<اسم عربي>` → 10 بطاقات لكل صفحة
  (`class="bs"`)؛ الأنواع في `TYPES` (مانهوا كورية، مانها صيني، …).
- `/series/<slug>` → التفاصيل + أول 40 فصلاً؛ باقي الفصول عبر `?page=N`
  (عدد الصفحات من شريط الترقيم، بحد أقصى 25 صفحة).
- `/series/<slug>/<رقم الفصل>` → الصور (`class="manga-chapter-img"`).
- `/ajax/search?keyword=` → بحث (يفهم اللاتينية فقط؛ العربي يرجع فارغاً).
معرّف الفصل: `tx:<slug>:<رقم>`. صور الموقع تمر عبر `/img` مع Referer الموقع
(`isTxImage`) — لا تحذف ذلك وإلا ظهرت أغلفة مكسورة.

مواقع اختُبرت ورُفضت (2026-08-22): mangalik.net و like-manga.net (Madara)
تُرجعان 403 من Cloudflare على `/manga/*`؛ azorafly.com يُرجع 504 على `/series/`؛
gmanga/swatmanga/teamx.fun نطاقاتها بيعت أو تحوّلت. لا تُعِد تجربتها بلا سبب.
